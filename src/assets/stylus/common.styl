// 防止文本溢出
text-overflow(n)
    overflow hidden
    text-overflow ellipsis
    if n > 1
        display -webkit-box
        -webkit-line-clamp n
        -webkit-box-orient vertical
    else
        white-space nowrap
// page title
.common-desc
    width 100%
    height 50px
    line-height 50px
    margin 20px 0
    color #333
    font-size 20px
    text-align center
    font-weight 600